/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['lorem.picsum.photos', 'picsum.photos', "www.w3schools.com", "w3schools.com"],
  },
}

module.exports = nextConfig
